# WebCore Audit Environment Variables

# Required: Anthropic API Key for Claude AI analysis
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# NextAuth (for admin authentication)
NEXTAUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_URL=http://localhost:3000

# Optional: Google PageSpeed API (for future enhanced checks)
# GOOGLE_PAGESPEED_API_KEY=

# Optional: Email service (Resend) for sending reports
# RESEND_API_KEY=

# App URL (for production)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================
# OAuth Provider Setup (Configure in Supabase Dashboard)
# =============================================
#
# Google OAuth (Currently Enabled):
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Go to APIs & Services > Credentials
# 4. Click "Create Credentials" > "OAuth client ID"
# 5. Select "Web application"
# 6. Add authorized redirect URI: https://YOUR_PROJECT.supabase.co/auth/v1/callback
# 7. Copy Client ID and Client Secret
# 8. Go to Supabase Dashboard > Authentication > Providers > Google
# 9. Enable Google and paste your Client ID and Client Secret
#
# =============================================
# ROADMAP: Future OAuth Providers
# =============================================
#
# Apple OAuth (Coming Soon):
# 1. Go to https://developer.apple.com/
# 2. Create an App ID and Services ID
# 3. Configure Sign in with Apple
# 4. Add credentials in Supabase Dashboard > Auth > Providers > Apple
#
# Facebook OAuth (Coming Soon):
# 1. Go to https://developers.facebook.com/
# 2. Create an app and configure Facebook Login
# 3. Add redirect URI: https://YOUR_PROJECT.supabase.co/auth/v1/callback
# 4. Add App ID and Secret in Supabase Dashboard > Auth > Providers > Facebook
